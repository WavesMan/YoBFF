openapi: 3.0.3
info:
  title: YoBFF 控制面 API
  version: 1.0.0
servers:
  - url: http://localhost:9090
security:
  - BearerAuth: []
paths:
  /healthz:
    get:
      summary: 健康检查
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/login:
    post:
      summary: 管理员登录
      description: 验证用户名密码并返回 API Token
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - username
                - password
              properties:
                username:
                  type: string
                  example: admin
                password:
                  type: string
                  example: password
                captcha_id:
                  type: string
                  description: 需要验证码时填写，由 /api/v1/captcha 获取
                  example: id-1
                captcha_code:
                  type: string
                  description: 需要验证码时填写，用户输入的验证码文本
                  example: "1234"
      responses:
        "200":
          description: 登录成功
          content:
            application/json:
              schema:
                type: object
                properties:
                  token:
                    type: string
                    example: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
                  operator:
                    type: string
                    description: 当前登录用户标识
                    example: admin
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/login/require-captcha:
    get:
      summary: 查询登录是否需要验证码
      description: 根据当前客户端失败状态判断是否要求验证码
      security: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  required:
                    type: boolean
                    example: false
        "405":
          $ref: "#/components/responses/MethodNotAllowed"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/logout:
    post:
      summary: 管理员登出
      description: 提示客户端清理令牌
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: logged_out
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/captcha:
    get:
      summary: 获取验证码
      description: 生成一次性验证码图片（Base64），用于登录人机校验
      security: []
      parameters:
        - in: query
          name: preset
          schema:
            type: string
            enum:
              - easy
              - default
          required: false
          description: 验证码预设，easy 更易识读
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CaptchaResponse"
        "405":
          $ref: "#/components/responses/MethodNotAllowed"
        "503":
          $ref: "#/components/responses/CaptchaUnavailable"
  /api/v1/config:
    get:
      summary: 获取当前配置
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Config"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
    put:
      summary: 更新并应用配置
      parameters:
        - in: header
          name: X-Operator
          schema:
            type: string
          required: false
          description: 操作人标识
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Config"
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: updated
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/config/validate:
    post:
      summary: 预检配置合法性
      parameters:
        - in: header
          name: X-Operator
          schema:
            type: string
          required: false
          description: 操作人标识
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Config"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigValidationResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/config/versions:
    get:
      summary: 查询配置版本列表
      parameters:
        - in: query
          name: limit
          schema:
            type: integer
            format: int32
            default: 20
          required: false
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigVersionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/config/rollback:
    post:
      summary: 回滚到历史配置版本
      parameters:
        - in: header
          name: X-Operator
          schema:
            type: string
          required: false
          description: 操作人标识
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConfigRollbackRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigRollbackResponse"
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/config/cdn:
    get:
      summary: 获取 CDN 同步配置与状态
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  config:
                    $ref: "#/components/schemas/CDNSyncConfig"
                  status:
                    type: object
                    additionalProperties:
                      $ref: "#/components/schemas/CDNStatus"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
    put:
      summary: 更新 CDN 同步配置
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CDNSyncConfig"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: updated
                  config:
                    $ref: "#/components/schemas/CDNSyncConfig"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/config/reload:
    post:
      summary: 触发配置热重载
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: reloaded
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/lb/pools:
    get:
      summary: 获取流量池列表
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LBPoolListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
    post:
      summary: 创建流量池
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LBPool"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: created
                  id:
                    type: string
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/lb/pools/{pool_id}:
    parameters:
      - in: path
        name: pool_id
        schema:
          type: string
        required: true
    get:
      summary: 获取流量池详情
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LBPool"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/LBPoolNotFound"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
    put:
      summary: 更新流量池
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LBPool"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: updated
                  pool_id:
                    type: string
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/LBPoolNotFound"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
    delete:
      summary: 删除流量池
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: deleted
                  pool_id:
                    type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/LBPoolNotFound"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/lb/routes:
    get:
      summary: 获取域名绑定规则列表
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LBRouteListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/lb/routes/{domain}:
    parameters:
      - in: path
        name: domain
        schema:
          type: string
        required: true
    get:
      summary: 获取域名绑定规则详情
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LBRouteRule"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/LBRouteNotFound"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
    put:
      summary: 新增或更新域名绑定规则
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LBRouteRule"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: updated
                  domain:
                    type: string
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
    delete:
      summary: 删除域名绑定规则
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: deleted
                  domain:
                    type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/LBRouteNotFound"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/site-groups:
    get:
      summary: 按 hostname 或 IP 分类站点
      parameters:
        - in: query
          name: by
          schema:
            type: string
            enum:
              - hostname
              - ip
          required: true
          description: 分类维度
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteGroupResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/sites:
    get:
      summary: 获取站点列表
      parameters:
        - in: query
          name: hostname
          schema:
            type: string
          required: false
          description: 按 hostname 精确过滤
        - in: query
          name: ip
          schema:
            type: string
          required: false
          description: 按 IP 精确过滤
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
    post:
      summary: 创建站点
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SiteCreateRequest"
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Site"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/sites/{site_id}:
    parameters:
      - in: path
        name: site_id
        schema:
          type: string
        required: true
    get:
      summary: 获取站点详情
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Site"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/SiteNotFound"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
    put:
      summary: 更新站点信息
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SiteUpdateRequest"
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Site"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/SiteNotFound"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
    delete:
      summary: 删除站点
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Site"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/SiteNotFound"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/certs:
    get:
      summary: 获取 SSL 证书列表
      parameters:
        - in: query
          name: page
          schema:
            type: integer
            default: 1
          required: false
        - in: query
          name: pageSize
          schema:
            type: integer
            default: 15
          required: false
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CertificateListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
    post:
      summary: 上传 SSL 证书
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                cert:
                  type: string
                  format: binary
                key:
                  type: string
                  format: binary
                name:
                  type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SSLCertificate"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"
        "500":
          $ref: "#/components/responses/InternalError"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/certs/{id}:
    delete:
      summary: 删除 SSL 证书
      parameters:
        - in: path
          name: id
          schema:
            type: string
          required: true
      responses:
        "204":
          description: No Content
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"
        "500":
          $ref: "#/components/responses/InternalError"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/sites/{site_id}/config:
    parameters:
      - in: path
        name: site_id
        schema:
          type: string
        required: true
    get:
      summary: 获取站点当前配置
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Config"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/SiteNotFound"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
    put:
      summary: 更新站点配置
      parameters:
        - in: header
          name: X-Operator
          schema:
            type: string
          required: false
          description: 操作人标识
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Config"
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigVersion"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/sites/{site_id}/config/validate:
    post:
      summary: 站点配置预检
      parameters:
        - in: path
          name: site_id
          schema:
            type: string
          required: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Config"
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigValidationResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/sites/{site_id}/config/diff:
    post:
      summary: 站点配置差异比对
      parameters:
        - in: path
          name: site_id
          schema:
            type: string
          required: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Config"
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigDiffResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/SiteNotFound"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/sites/{site_id}/config/versions:
    get:
      summary: 获取站点配置版本列表
      parameters:
        - in: path
          name: site_id
          schema:
            type: string
          required: true
        - in: query
          name: limit
          schema:
            type: integer
            format: int32
          required: false
          description: 返回数量上限
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigVersionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/SiteNotFound"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/sites/{site_id}/config/versions/{version_id}:
    get:
      summary: 获取站点版本配置详情
      parameters:
        - in: path
          name: site_id
          schema:
            type: string
          required: true
        - in: path
          name: version_id
          schema:
            type: string
          required: true
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Config"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/VersionNotFound"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
    delete:
      summary: 删除站点配置历史版本
      parameters:
        - in: path
          name: site_id
          schema:
            type: string
          required: true
        - in: path
          name: version_id
          schema:
            type: string
          required: true
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteVersionDeleteResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/VersionNotFound"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/sites/{site_id}/config/rollback:
    post:
      summary: 回滚站点配置版本
      parameters:
        - in: path
          name: site_id
          schema:
            type: string
          required: true
        - in: header
          name: X-Operator
          schema:
            type: string
          required: false
          description: 操作人标识
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConfigRollbackRequest"
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigRollbackResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/VersionNotFound"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/sites/{site_id}/log/stream:
    get:
      summary: 获取站点日志流探查配置
      parameters:
        - in: path
          name: site_id
          schema:
            type: string
          required: true
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteLogStream"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/LogStreamNotFound"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/sites/{site_id}/log/history:
    get:
      summary: 查询站点历史日志
      parameters:
        - in: path
          name: site_id
          schema:
            type: string
          required: true
        - in: query
          name: kind
          schema:
            type: string
            enum:
              - all
              - traffic
              - system
          required: false
          description: 日志类型过滤，默认 all
        - in: query
          name: level
          schema:
            type: string
            enum:
              - debug
              - info
              - warn
              - error
          required: false
          description: 日志等级过滤
        - in: query
          name: start_time
          schema:
            type: string
            format: date-time
          required: false
          description: 起始时间，RFC3339 格式
        - in: query
          name: end_time
          schema:
            type: string
            format: date-time
          required: false
          description: 结束时间，RFC3339 格式
        - in: query
          name: limit
          schema:
            type: integer
            format: int32
            minimum: 1
            maximum: 500
            default: 100
          required: false
          description: 返回条数上限
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteLogHistoryResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/SiteNotFound"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
    put:
      summary: 更新站点日志流探查配置
      parameters:
        - in: path
          name: site_id
          schema:
            type: string
          required: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SiteLogStream"
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteLogStream"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/SiteNotFound"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/sites/{site_id}/cdn/origin/status:
    get:
      summary: 获取站点回源 IP 同步状态
      parameters:
        - in: path
          name: site_id
          schema:
            type: string
          required: true
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteCDNOriginStatusListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/SiteNotFound"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/sites/{site_id}/cdn/origin/refresh:
    post:
      summary: 触发站点回源 IP 立即拉取
      parameters:
        - in: path
          name: site_id
          schema:
            type: string
          required: true
        - in: header
          name: X-Operator
          schema:
            type: string
          required: false
          description: 操作人标识
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SiteCDNOriginRefreshRequest"
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteCDNOriginRefreshResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/SiteNotFound"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/log/level:
    get:
      summary: 查询当前日志级别
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                type: object
                properties:
                  level:
                    type: string
                    example: info
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
    put:
      summary: 更新日志级别
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LogLevelUpdate"
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: updated
                  level:
                    type: string
                    example: info
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/log/stats:
    get:
      summary: 获取日志管线统计
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogStats"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/audit/logs:
    get:
      summary: 获取审计日志列表
      description: 支持按动作、操作人、目标和时间范围筛选，并返回分页结果。
      parameters:
        - in: query
          name: action
          schema:
            type: string
          required: false
          description: 按动作类型精确筛选
        - in: query
          name: operator
          schema:
            type: string
          required: false
          description: 按操作人精确筛选
        - in: query
          name: target
          schema:
            type: string
          required: false
          description: 按目标标识精确筛选
        - in: query
          name: start_time
          schema:
            type: string
            format: date-time
          required: false
          description: 起始时间，RFC3339 格式
        - in: query
          name: end_time
          schema:
            type: string
            format: date-time
          required: false
          description: 结束时间，RFC3339 格式
        - in: query
          name: limit
          schema:
            type: integer
            format: int32
            minimum: 1
            maximum: 500
          required: false
          description: 兼容参数，未传 pageSize 时作为每页数量使用
        - in: query
          name: page
          schema:
            type: integer
            format: int32
            minimum: 1
            default: 1
          required: false
          description: 页码，从 1 开始
        - in: query
          name: pageSize
          schema:
            type: integer
            format: int32
            minimum: 1
            maximum: 500
            default: 15
          required: false
          description: 每页条数
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditLogListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/weaver/drafts:
    get:
      summary: 获取 Weaver 草稿列表
      description: 返回可视化聚合编排草稿列表，按创建时间倒序排列。
      parameters:
        - in: query
          name: limit
          schema:
            type: integer
            format: int32
            minimum: 1
            maximum: 200
            default: 50
          required: false
          description: 返回条数上限
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeaverDraftListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
    post:
      summary: 创建 Weaver 草稿
      description: 创建用于可视化 API 聚合编排的草稿。
      parameters:
        - in: header
          name: X-Operator
          schema:
            type: string
          required: false
          description: 操作人标识，未提供时自动回退到登录用户
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WeaverDraftRequest"
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeaverDraft"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/weaver/drafts/{draft_id}:
    parameters:
      - in: path
        name: draft_id
        schema:
          type: string
        required: true
        description: 草稿标识
    get:
      summary: 获取 Weaver 草稿详情
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeaverDraft"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/WeaverDraftNotFound"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
    put:
      summary: 更新 Weaver 草稿
      parameters:
        - in: header
          name: X-Operator
          schema:
            type: string
          required: false
          description: 操作人标识，未提供时自动回退到登录用户
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WeaverDraftRequest"
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeaverDraft"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/WeaverDraftNotFound"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
    delete:
      summary: 删除 Weaver 草稿
      parameters:
        - in: header
          name: X-Operator
          schema:
            type: string
          required: false
          description: 操作人标识，未提供时自动回退到登录用户
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeaverDeleteResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/WeaverDraftNotFound"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
  /api/v1/weaver/drafts/{draft_id}/run:
    parameters:
      - in: path
        name: draft_id
        schema:
          type: string
        required: true
        description: 草稿标识
    post:
      summary: 运行 Weaver 草稿
      description: 使用草稿内输入与映射进行试跑，也可在请求体覆盖输入或映射。
      parameters:
        - in: header
          name: X-Operator
          schema:
            type: string
          required: false
          description: 操作人标识，未提供时自动回退到登录用户
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WeaverRunRequest"
      responses:
        "200":
          description: OK
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            X-RateLimit-Limit:
              $ref: "#/components/headers/RateLimitLimit"
            X-RateLimit-Remaining:
              $ref: "#/components/headers/RateLimitRemaining"
            X-RateLimit-Reset:
              $ref: "#/components/headers/RateLimitReset"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeaverRunResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/WeaverDraftNotFound"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/AuthUnavailable"
components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
  headers:
    RequestID:
      description: 请求追踪标识
      schema:
        type: string
    RateLimitLimit:
      description: 当前时间窗口最大请求数
      schema:
        type: integer
        format: int32
    RateLimitRemaining:
      description: 当前窗口剩余请求数
      schema:
        type: integer
        format: int32
    RateLimitReset:
      description: 窗口重置时间戳
      schema:
        type: integer
        format: int64
  responses:
    BadRequest:
      description: 请求非法
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Unauthorized:
      description: 鉴权失败
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    RateLimited:
      description: 触发限流
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    InternalError:
      description: 服务内部错误
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    AuthUnavailable:
      description: 鉴权未配置
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    CaptchaUnavailable:
      description: 验证码服务不可用
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    MethodNotAllowed:
      description: 请求方法不允许
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    ValidationFailed:
      description: 配置预检失败
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ValidationErrorResponse"
    LBPoolNotFound:
      description: 流量池不存在
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            error_code: pool_not_found
            message: pool not found
            request_id: req-123456
    LBRouteNotFound:
      description: 域名绑定规则不存在
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            error_code: route_not_found
            message: route not found
            request_id: req-123456
    SiteNotFound:
      description: 站点不存在
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            error_code: site_not_found
            message: site not found
            request_id: req_109238475a
    VersionNotFound:
      description: 站点配置版本不存在
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            error_code: version_not_found
            message: version not found
            request_id: req_109238475b
    LogStreamNotFound:
      description: 站点日志流配置不存在
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            error_code: log_stream_not_found
            message: log stream not found
            request_id: req_109238475c
    WeaverDraftNotFound:
      description: Weaver 草稿不存在
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            error_code: draft_not_found
            message: weaver draft not found
            request_id: req_109238475w
  schemas:
    ErrorResponse:
      type: object
      required:
        - error_code
        - message
        - request_id
      properties:
        error_code:
          type: string
        message:
          type: string
        request_id:
          type: string
    AuditLogEntry:
      type: object
      required:
        - id
        - action
        - created_at
      properties:
        id:
          type: string
        action:
          type: string
        target:
          type: string
        created_at:
          type: string
          format: date-time
        operator:
          type: string
        detail:
          type: string
    AuditLogListResponse:
      type: object
      required:
        - items
        - total
        - page
        - pageSize
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/AuditLogEntry"
        total:
          type: integer
          format: int64
        page:
          type: integer
          format: int32
        pageSize:
          type: integer
          format: int32
    WeaverInputPayload:
      type: object
      required:
        - name
        - payload
      properties:
        name:
          type: string
        payload:
          type: object
          additionalProperties: true
    WeaverDraftRequest:
      type: object
      required:
        - name
        - inputs
        - mapping
      properties:
        name:
          type: string
        inputs:
          type: array
          items:
            $ref: "#/components/schemas/WeaverInputPayload"
        mapping:
          type: object
          additionalProperties: true
    WeaverDraft:
      type: object
      required:
        - id
        - name
        - inputs
        - mapping
        - created_at
        - updated_at
      properties:
        id:
          type: string
        name:
          type: string
        inputs:
          type: array
          items:
            $ref: "#/components/schemas/WeaverInputPayload"
        mapping:
          type: object
          additionalProperties: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        operator:
          type: string
    WeaverDraftListResponse:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/WeaverDraft"
    WeaverRunRequest:
      type: object
      properties:
        inputs:
          type: array
          items:
            $ref: "#/components/schemas/WeaverInputPayload"
        mapping:
          type: object
          additionalProperties: true
    WeaverRunSource:
      type: object
      required:
        - name
        - ok
      properties:
        name:
          type: string
        ok:
          type: boolean
        error:
          type: string
    WeaverRunResponse:
      type: object
      required:
        - output
        - duration_ms
        - sources
      properties:
        output:
          type: object
          additionalProperties: true
        duration_ms:
          type: integer
          format: int64
        sources:
          type: array
          items:
            $ref: "#/components/schemas/WeaverRunSource"
    WeaverDeleteResponse:
      type: object
      required:
        - status
        - id
      properties:
        status:
          type: string
          enum:
            - deleted
        id:
          type: string
    ValidationIssue:
      type: object
      required:
        - path
        - message
      properties:
        path:
          type: string
        message:
          type: string
    ValidationErrorResponse:
      type: object
      required:
        - error_code
        - message
        - request_id
        - errors
      properties:
        error_code:
          type: string
          example: config_invalid
        message:
          type: string
        request_id:
          type: string
        errors:
          type: array
          items:
            $ref: "#/components/schemas/ValidationIssue"
    ConfigValidationResponse:
      type: object
      required:
        - valid
        - errors
      properties:
        valid:
          type: boolean
        errors:
          type: array
          items:
            $ref: "#/components/schemas/ValidationIssue"
    ConfigVersion:
      type: object
      required:
        - id
        - created_at
      properties:
        id:
          type: string
        created_at:
          type: string
          format: date-time
        operator:
          type: string
        source:
          type: string
    ConfigVersionResponse:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ConfigVersion"
    ConfigRollbackRequest:
      type: object
      required:
        - version_id
      properties:
        version_id:
          type: string
    ConfigRollbackResponse:
      type: object
      properties:
        status:
          type: string
        version_id:
          type: string
    SiteVersionDeleteResponse:
      type: object
      required:
        - status
        - version_id
      properties:
        status:
          type: string
          example: deleted
        version_id:
          type: string
          example: 8d0de2d4a6f64c5c83f33c4b49b8a3c1
    Site:
      type: object
      required:
        - id
        - name
        - hostname
        - ip
      properties:
        id:
          type: string
        name:
          type: string
        hostname:
          type: string
        ip:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    SiteSummary:
      type: object
      required:
        - id
        - name
        - hostname
        - ip
      properties:
        id:
          type: string
        name:
          type: string
        hostname:
          type: string
        ip:
          type: string
    SiteCreateRequest:
      type: object
      required:
        - name
        - hostname
        - ip
      properties:
        name:
          type: string
        hostname:
          type: string
        ip:
          type: string
    SiteUpdateRequest:
      type: object
      properties:
        name:
          type: string
        hostname:
          type: string
        ip:
          type: string
    SiteListResponse:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Site"
    SiteGroup:
      type: object
      required:
        - group_key
        - count
        - sites
      properties:
        group_key:
          type: string
        count:
          type: integer
          format: int32
        sites:
          type: array
          items:
            $ref: "#/components/schemas/SiteSummary"
    SiteGroupResponse:
      type: object
      required:
        - groups
      properties:
        groups:
          type: array
          items:
            $ref: "#/components/schemas/SiteGroup"
    SiteLogStream:
      type: object
      required:
        - site_id
        - filter_query
      properties:
        site_id:
          type: string
        filter_query:
          type: string
        updated_at:
          type: string
          format: date-time
    SiteLogEntry:
      type: object
      required:
        - id
        - site_id
        - kind
        - level
        - message
        - created_at
      properties:
        id:
          type: string
        site_id:
          type: string
        kind:
          type: string
          enum:
            - traffic
            - system
        level:
          type: string
          example: info
        message:
          type: string
        request_id:
          type: string
        client_ip:
          type: string
        host:
          type: string
        method:
          type: string
          example: GET
        path:
          type: string
          example: /healthz
        status_code:
          type: integer
          format: int32
        latency_ms:
          type: integer
          format: int64
        operator:
          type: string
        action:
          type: string
        detail:
          type: string
        created_at:
          type: string
          format: date-time
    SiteLogHistoryResponse:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/SiteLogEntry"
    ConfigDiffItem:
      type: object
      required:
        - path
        - before
        - after
      properties:
        path:
          type: string
        before:
          type: object
        after:
          type: object
    ConfigDiffResponse:
      type: object
      required:
        - changes
      properties:
        changes:
          type: array
          items:
            $ref: "#/components/schemas/ConfigDiffItem"
    CertificateListResponse:
      type: object
      required:
        - items
        - total
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/SSLCertificate"
        total:
          type: integer
          format: int64
        page:
          type: integer
          format: int32
    SSLCertificate:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        domains:
          type: array
          items:
            type: string
        notAfter:
          type: string
          format: date-time
        issuer:
          type: string
        createdAt:
          type: string
          format: date-time
    Config:
      type: object
      properties:
        dataPlane:
          $ref: "#/components/schemas/DataPlaneConfig"
        controlPlane:
          $ref: "#/components/schemas/ControlPlaneConfig"
        security:
          $ref: "#/components/schemas/SecurityConfig"
        routing:
          $ref: "#/components/schemas/RoutingConfig"
        loadBalancer:
          $ref: "#/components/schemas/LoadBalancerConfig"
        cdnSync:
          $ref: "#/components/schemas/CDNSyncConfig"
        certificates:
          type: array
          items:
            $ref: "#/components/schemas/Certificate"
        sslCertificates:
          type: array
          items:
            $ref: "#/components/schemas/SSLCertificate"
    CDNSyncConfig:
      type: object
      properties:
        enabled:
          type: boolean
        providers:
          type: array
          items:
            type: string
        schedule:
          type: string
        cloudflare:
          $ref: "#/components/schemas/CloudflareConfig"
    CloudflareConfig:
      type: object
      properties:
        endpoint:
          type: string
        api_token:
          type: string
        zone_id:
          type: string
    CDNStatus:
      type: object
      properties:
        provider:
          type: string
        cidrs:
          type: array
          items:
            type: string
        last_sync:
          type: string
          format: date-time
        error:
          type: string
    CaptchaResponse:
      type: object
      required:
        - captcha_id
        - image_base64
      properties:
        captcha_id:
          type: string
        image_base64:
          type: string
        preset:
          type: string
    DataPlaneConfig:
      type: object
      properties:
        httpListenAddr:
          type: string
        httpsListenAddr:
          type: string
        enableHttps:
          type: boolean
    ControlPlaneConfig:
      type: object
      properties:
        adminListenAddr:
          type: string
        auth:
          type: object
          properties:
            username:
              type: string
            password:
              type: string
            token:
              type: string
    SecurityConfig:
      type: object
      properties:
        allowedCidrs:
          type: array
          items:
            type: string
        trustedProxyCidrs:
          type: array
          items:
            type: string
        allowedCdnProviders:
          type: array
          items:
            type: string
        cdnProviderSettings:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/CDNProviderSetting"
        originProtectionMode:
          type: string
          enum:
            - disabled
            - enforced
        enableHsts:
          type: boolean
    CDNProviderSetting:
      type: object
      properties:
        apiKey:
          type: string
        secretKey:
          type: string
          writeOnly: true
        option:
          type: string
        accountId:
          type: string
        zoneId:
          type: string
        endpoint:
          type: string
        refreshIntervalSeconds:
          type: integer
          format: int32
        maxStalenessSeconds:
          type: integer
          format: int32
    SiteCDNOriginStatus:
      type: object
      required:
        - site_id
        - provider
        - consecutive_failures
        - updated_at
      properties:
        site_id:
          type: string
        provider:
          type: string
        last_attempt_at:
          type: string
          format: date-time
        last_success_at:
          type: string
          format: date-time
        consecutive_failures:
          type: integer
          format: int32
        last_error:
          type: string
        updated_at:
          type: string
          format: date-time
    SiteCDNOriginStatusListResponse:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/SiteCDNOriginStatus"
    SiteCDNOriginRefreshRequest:
      type: object
      properties:
        providers:
          type: array
          items:
            type: string
          description: 可选，指定需要刷新的 CDN 厂商列表；为空时使用站点配置 allowedCdnProviders
    SiteCDNOriginRefreshResult:
      type: object
      required:
        - provider
        - ok
      properties:
        provider:
          type: string
        ok:
          type: boolean
        count:
          type: integer
          format: int32
        error:
          type: string
    SiteCDNOriginRefreshResponse:
      type: object
      required:
        - status
        - results
      properties:
        status:
          type: string
          example: refreshed
        results:
          type: array
          items:
            $ref: "#/components/schemas/SiteCDNOriginRefreshResult"
    RoutingConfig:
      type: object
      properties:
        defaultUpstream:
          type: string
        domains:
          type: array
          items:
            $ref: "#/components/schemas/DomainRule"
    LoadBalancerConfig:
      type: object
      required:
        - pools
        - routes
      properties:
        defaultPoolId:
          type: string
        pools:
          type: array
          items:
            $ref: "#/components/schemas/LBPool"
        routes:
          type: array
          items:
            $ref: "#/components/schemas/LBRouteRule"
    LBPoolListResponse:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/LBPool"
    LBRouteListResponse:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/LBRouteRule"
        default_pool_id:
          type: string
    LBPool:
      type: object
      required:
        - id
        - name
        - strategy
        - nodes
      properties:
        id:
          type: string
          example: pool_main
        name:
          type: string
          example: 主池
        strategy:
          type: string
          example: weighted_rr
        nodes:
          type: array
          items:
            $ref: "#/components/schemas/LBNode"
    LBNode:
      type: object
      required:
        - id
        - upstream
        - weight
        - enabled
      properties:
        id:
          type: string
          example: node_a
        upstream:
          type: string
          example: http://127.0.0.1:8080
        weight:
          type: integer
          format: int32
          example: 10
        enabled:
          type: boolean
          example: true
    LBRouteRule:
      type: object
      required:
        - domain
        - poolId
        - forceHttps
      properties:
        domain:
          type: string
          example: api.example.com
        poolId:
          type: string
          example: pool_main
        fallbackPoolId:
          type: string
          example: pool_backup
        forceHttps:
          type: boolean
          example: true
    DomainRule:
      type: object
      properties:
        domain:
          type: string
        upstream:
          type: string
        forceHttps:
          type: boolean
    Certificate:
      type: object
      properties:
        domain:
          type: string
        certFile:
          type: string
        keyFile:
          type: string
        certPem:
          type: string
        keyPem:
          type: string
    LogLevelUpdate:
      type: object
      required:
        - level
      properties:
        level:
          type: string
    LogStats:
      type: object
      properties:
        total:
          type: integer
          format: uint64
        blocked:
          type: integer
          format: uint64
        proxied:
          type: integer
          format: uint64
        dropped:
          type: integer
          format: uint64
